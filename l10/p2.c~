#include <stdio.h>

typedef struct{
  int ora;
  double temp;
}rec;

int main(){
  int n=0;
  rec v[10];

  scanf("%d",&n);
  
  for(int i=0 ; i<n ; i++){
    scanf("%d %lf",&v[i].ora,&v[i].temp);
  }

  printf("%d",v[0].ora);
  scanf("%d",&n);
}
